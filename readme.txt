the output file contains the final result
the transfer name function is used to transfer input data to UTF8 type;
the CommercialGroup.jar is the main part of the project;
the 1 file is a sample input data of the size 5MB;
the gather file function is to gather 1000files into 3 files to see the difference of performance.
Please use a ultraEdit to open the txt files. Because the default txt editor cannot represent the separators well.